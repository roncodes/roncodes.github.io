import config from 'personal/utils/config';
import { module, test } from 'qunit';

module('Unit | Utility | config', function () {
	// TODO: Replace this with your real tests.
	test('it works', function (assert) {
		let result = config();
		assert.ok(result);
	});
});
